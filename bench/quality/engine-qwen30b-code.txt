model qwen30b | corpus code | 512 positions to score
[console.error] Failed to load resource: the server responded with a status of 404 (Not Found)

  tokens scored  512
  mean NLL       3.0861
  PERPLEXITY     21.892  [16.545, 28.968]
  19.6 positions/s, 26s, gpu errors 0

  NOT COMPARABLE ACROSS --tokens: this scores one contiguous prefix, so the
  number falls steeply with window length (5x between 128 and 1198 on a 1B).
  Quote it only against another run at --tokens 512 on --corpus code.
  To compare two CHECKPOINTS, use scripts/quality-ab.py — independent
  windows, error bars, and it runs on the reference in seconds.

  uniform-over-vocab perplexity would be 151936 (qwen30b vocab)

  vs mlx_lm on the SAME ids (float32):
    mlx perplexity     21.809
    engine perplexity  21.892
    ratio              1.0038
    max |ΔNLL| at any position  6.85e-1
  (fidelity, over 512 positions rather than one prompt's final logits.
   It says nothing about whether the model is good — both sides are the same
   quantized checkpoint.)
